@model IEnumerable<Clinic_System.Models.Tratamiento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaInicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaFin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Indicaciones)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Atencion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaInicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaFin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Indicaciones)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Atencion.IdAtencion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTratamiento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTratamiento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTratamiento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
